extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Elemento


      if lista 
        each a in lista
            div(class="w3-container w3-margin-left")
                p 
                b #{a.p} 
                |: #{a.o}  
 
