extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Grupos


      if lista
       ol 
        each a in lista
          li
            a(href=`/periodo/${a}`) #{a}