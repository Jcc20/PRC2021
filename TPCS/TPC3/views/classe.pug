extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Classes


      if lista
       ol 
        each a in lista
          li
            a(href=`/${id}/classe/${classe}/individuo/${a.s.value.split("#")[1]}`) #{a.s.value.split("#")[1]}
 
